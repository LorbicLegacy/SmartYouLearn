<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tech.villageprogrammer.apps.smartyoulearn">
    <application
        android:allowBackup="true"
        android:icon="@drawable/icon200px"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon200px"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AboutActivity"/>
        <activity android:name=".TopicDataActivity" />
        <activity android:name=".LoaderActivity" />
        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|layoutDirection|uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>